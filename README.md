# Driver Rating KZ Pro Bot

Telegram-бот для проверки водителей по госномеру и обмена отзывами.

## 🚀 Возможности

### Для пользователей:
- 🔍 Проверка водителей по госномеру
- ✍️ Оставление отзывов с фото/видео и геолокацией
- 🚗 Гараж - отслеживание отзывов на свои авто
- 💎 Платные подписки с расширенными возможностями
- 🎁 Реферальная программа

### Для админа:
- 📊 Подробная статистика
- 📢 Рассылка сообщений
- 🔍 Поиск пользователей
- 💰 Управление платежами
- 🗑 Модерация контента
- 🚫 Бан/разбан пользователей

## 📦 Установка

### 1. Клонируйте репозиторий
```bash
cd driver-rating-bot
```

### 2. Создайте виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения
```bash
cp .env.example .env
nano .env  # Отредактируйте файл
```

Обязательно заполните:
- `BOT_TOKEN` - токен вашего бота от @BotFather
- `ADMIN_ID` - ваш Telegram ID
- `DATABASE_URL` - URL подключения к PostgreSQL

### 5. Настройте PostgreSQL

Создайте базу данных:
```bash
createdb driver_rating_db
```

Или используйте существующую, указав URL в `.env`

### 6. Запустите бота
```bash
python bot.py
```

## 🏗 Структура проекта

```
driver-rating-bot/
├── bot.py                  # Главный файл
├── config.py               # Конфигурация
├── requirements.txt        # Зависимости
├── .env                    # Переменные окружения (не коммитить!)
├── .env.example            # Пример переменных
│
├── database/
│   └── db_manager.py       # Работа с БД
│
├── handlers/
│   ├── user_handlers.py    # Пользовательские команды
│   ├── payment_handlers.py # Платежи
│   └── admin_handlers.py   # Админ-панель
│
├── keyboards/
│   ├── inline_keyboards.py # Inline-клавиатуры
│   └── reply_keyboards.py  # Reply-клавиатуры
│
├── models/
│   └── subscription_tiers.py # Модели подписок
│
├── utils/
│   ├── logger.py           # Логирование
│   ├── validators.py       # Валидация данных
│   └── formatters.py       # Форматирование сообщений
│
└── tests/
    ├── unit/               # Юнит-тесты
    └── integration/        # Интеграционные тесты
```

## 💎 Тарифные планы

### 🆓 Бесплатный
- 3 поиска в день
- Только первый отзыв
- 1 авто в гараже

### ⭐ Базовый (500 ₸/мес)
- Безлимитные поиски
- Все отзывы
- До 3 авто в гараже

### 💎 Премиум (1000 ₸/мес)
- Все из Базового
- Безлимитный гараж
- Экспорт в PDF
- Аналитика
- Приоритетная поддержка

### 🏢 Бизнес (2500 ₸/мес)
- Все из Премиума
- API доступ (в разработке)
- Белый лейбл отчеты

## 🔒 Безопасность

- ✅ Все секреты в переменных окружения
- ✅ Валидация всех пользовательских данных
- ✅ Защита от SQL-инъекций (параметризованные запросы)
- ✅ Rate limiting для предотвращения спама
- ✅ Логирование всех важных действий

## 📊 Мониторинг

Логи сохраняются в `logs/bot.log` с автоматической ротацией.

Уровни логирования:
- `DEBUG` - детальная отладка
- `INFO` - обычная работа
- `WARNING` - предупреждения
- `ERROR` - ошибки
- `CRITICAL` - критические ошибки

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# С покрытием
pytest --cov=. --cov-report=html

# Только юнит-тесты
pytest tests/unit/
```

## 📈 Развертывание

### На VPS (Ubuntu/Debian)

1. Установите зависимости:
```bash
sudo apt update
sudo apt install python3 python3-pip postgresql
```

2. Создайте systemd service:
```bash
sudo nano /etc/systemd/system/driver-rating-bot.service
```

```ini
[Unit]
Description=Driver Rating KZ Pro Bot
After=network.target postgresql.service

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/driver-rating-bot
Environment="PATH=/path/to/driver-rating-bot/venv/bin"
ExecStart=/path/to/driver-rating-bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

3. Запустите сервис:
```bash
sudo systemctl enable driver-rating-bot
sudo systemctl start driver-rating-bot
sudo systemctl status driver-rating-bot
```

## 🤝 Поддержка

Если возникли вопросы, свяжитесь с разработчиком.

## 📝 Лицензия

Proprietary - все права защищены.
